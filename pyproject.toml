[project]
name = "DimondPricePrediction"
version = "0.0.1"
authors = [
    { name = "Dhiraj Chaware" }
]
dependencies = [
    "scikit-learn",
    "pandas",
    "numpy",
]

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"