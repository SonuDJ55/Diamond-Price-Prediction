[project]
name = "DimondPricePrediction"
version = "0.0.1"
authors = [
    { name = "Dhiraj Chaware" }
]
dependencies = [
    "scikit-learn",
    "pandas",
    "numpy",
    "fsspec>=2026.4.0",
    "seaborn>=0.13.2",
]

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"
